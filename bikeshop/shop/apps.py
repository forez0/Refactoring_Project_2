"""Конфігурація додатку 'shop'."""

from django.apps import AppConfig

class ShopConfig(AppConfig):
    """Конфігураційний клас для додатку 'shop'."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'shop'

